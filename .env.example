# Serenique Cloud Server Environment Variables
# Copy this file to .env and fill in your actual values

# =============================================================================
# OpenRouter API Configuration (REQUIRED)
# =============================================================================
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# =============================================================================
# Firebase Configuration (REQUIRED - Choose ONE method)
# =============================================================================

# Method 1: Paste entire service account JSON as a string
# (Recommended for Vercel deployment)
FIREBASE_CREDENTIALS='{"type":"service_account","project_id":"your-project","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}'

# Method 2: Path to service account JSON file
# (Recommended for local development)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/firebase-service-account.json

# =============================================================================
# Server Configuration (Optional)
# =============================================================================
# Port to run the server on (default: 8000)
# PORT=8000

# Environment mode (development/production)
# ENV=development

# =============================================================================
# Model Configuration (Optional)
# =============================================================================
# Change the AI model used for persona generation
# MODEL_NAME=anthropic/claude-3.5-sonnet
# MODEL_TEMPERATURE=0.7

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Get OpenRouter API Key:
#    - Go to https://openrouter.ai/
#    - Sign up or log in
#    - Navigate to "Keys" section
#    - Create a new API key
#    - Copy and paste it above
#
# 2. Get Firebase Service Account:
#    - Go to Firebase Console: https://console.firebase.google.com/
#    - Select your project (Serenique)
#    - Go to Project Settings (gear icon) → Service Accounts
#    - Click "Generate new private key"
#    - Save the JSON file as firebase-service-account.json
#    - Either:
#      a) Set GOOGLE_APPLICATION_CREDENTIALS to the file path, OR
#      b) Copy the entire JSON content and paste it as FIREBASE_CREDENTIALS
#
# 3. Copy this file:
#    cp .env.example .env
#
# 4. Edit .env with your actual values
#
# 5. Start the server:
#    uvicorn main:app --reload
#
# =============================================================================
